<h1>All available locations</h1>

<ul>
    {{#each locations}}
        <li><a href="/locations/{{id}}">{{name}}</a><button class="locDeleteBtn" data-locId={{id}}>Delete</button></li>
    {{/each}}
</ul>
<form id="newLocForm">
    <input type="text" placeholder="new location" name="newLoc">
    <button>Submit!</button>
</form>
<script src="/locationFetch.js"></script>